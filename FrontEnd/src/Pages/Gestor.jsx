import { Gestor_Endpoints } from "../Components/Gestor_Endpoints";

export function Gestor() {
    return (
        <main>
            {/* Renderizando a página dos endpoints do gestor */}
            <Gestor_Endpoints/>
        </main>
    )
}